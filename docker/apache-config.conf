<VirtualHost *:80>
    ServerName 127.0.0.1

    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

    ErrorLog ${APACHE_LOG_DIR}/chess-http-error.log
    CustomLog ${APACHE_LOG_DIR}/chess-http-access.log combined
</VirtualHost>
